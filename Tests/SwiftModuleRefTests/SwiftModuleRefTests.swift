import XCTest
@testable import SwiftModuleRef

final class SwiftModuleRefTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SwiftModuleRef().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
